[workspace]
members = [
    "core",
    "pybind",
    "tools/rule-tournament",
]
resolver = "3"

[workspace.dependencies]
clap = { version = "4.5", features = ["derive"] }

[profile.release]
codegen-units = 1
lto = "fat"
panic = "abort"

[profile.bench]
codegen-units = 1
lto = "fat"
