# Rust 引擎实现约定

[`GAME_RULES.md`](GAME_RULES.md) 是玩法和计分的唯一规范。本文只说明 Rust 引擎为实现确定性、可重放性和信息隔离而采用的实现约定：状态机、step 语义、固定动作空间、信息边界和计分执行。

当前 `ENGINE_RULES_VERSION` 为 `10`。版本 10 锁定首次和牌的完整基础结构，并恢复参考实现中的胡后摸切、自家暗杠/碰杠、花猪和查大叫语义。版本 7、8 和 9 的重放轨迹、checkpoint 和模型不能与当前引擎混用。训练必须从新的监督 Actor 和新的 PPO run 开始。

## 职责边界

见`engine/core`：

- `Game` 保存单局完整状态并执行游戏规则；
- 手牌分析器判断胡牌结构、向听数、有效牌和最大待牌倍率；
- `Batch` 批量执行多个独立 `Game`；
- `simple_rule_action` 和 `rule_ev_action` 是两种内置规则策略入口；
- feature `rule-nn` 的 `RuleNn::action` 是固定契约 ONNX 策略入口；
- observation 和事件接口为指定观察者过滤私有信息。

`Game` 为模拟、测试和重放公开全知状态。全知接口不等于策略可见信息。部署策略必须只使用当前行动者的私有状态、公开状态和观察者视角事件（见"信息边界"）。

## 状态机总览

引擎只在需要外部选择时停下。阶段流转如下：

```text
Exchange ─► ChooseMissing ─► Turn
Turn ─弃牌─┬─有胡候选► HuResponse ─有人胡► 结算胡牌
           │                         └─无人胡► 最近面子或下家 Turn
           └─无胡候选► MeldResponse ─────────► 最近面子或下家 Turn
结算胡牌或面子动作后 ─► Turn

Turn 内行动者可直接胡（含自摸、杠上开花）、暗杠或碰杠。胡牌结算后进入下一名玩家的 Turn；杠牌成立后由原行动者补牌并继续 Turn。
任意阶段牌墙耗尽 ──► Finished
```

| `Phase` | 外部选择 |
| --- | --- |
| `Exchange` | 选择一张换出牌 |
| `ChooseMissing` | 选择缺门 |
| `Turn` | 弃牌、胡、暗杠或碰杠 |
| `HuResponse` | 胡、碰、直杠或过；抢杠胡时只有胡或过 |
| `MeldResponse` | 碰、直杠或过 |
| `Finished` | 无后续动作 |

`Turn` 中的弃牌会为所有有资格响应的玩家打开一个响应窗口。只要至少一人可以胡，阶段为 `HuResponse`；每个候选玩家只选择一次，当前玩家可以在同一个窗口中选择胡、碰、直杠或过。选择过表示放弃该弃牌上的全部动作。只要任一玩家选择胡，所有碰和直杠都取消，并且可以一炮多响。没有任何胡候选时才进入 `MeldResponse`，其中只提供碰、直杠和过。抢杠胡使用 `HuResponse`，但只提供胡和过。自摸胡、杠上开花在 `Turn` 内直接由行动者发起。

## 确定性约定

- 座位编号为 `0..3`。庄家固定为座位 `0`。正常行牌按座位编号递增并循环。
- 洗牌和换三张方向只由局 seed 决定。换牌方向在左、对家、右之间等概率选择。
- 相同引擎版本、相同 seed 和相同动作序列必须产生相同状态、事件和分数。
- 换三张按座位 `0..3` 收集。每名玩家连续选择三张牌。第一张确定花色，后两张只能选择同花色且仍持有的牌。收齐 12 个选择后统一交换。
- 定缺按座位 `0..3` 收集。四名玩家都提交后统一公开。
- 一张弃牌按出牌者下家起的座次收集每名候选玩家的一次响应。只要存在胡候选，所有候选玩家都在同一个 `HuResponse` 中选择胡、碰、直杠或过；`Pass` 拒绝该弃牌上的全部动作。
- 只要至少一人选择胡，所有碰和直杠都取消；可以一炮多响。
- 没有胡候选时才使用 `MeldResponse`。该窗口只收集碰、直杠或过。
- 响应窗口结束时如果没有人选择胡，直杠优先于碰。同一种面子动作有多名候选时，按点炮者下家起的座次选择最近者。其他玩家的面子选择不再重复询问。
- 抢杠胡使用相同的多人胡牌流程。有人胡时，碰杠取消且不结算杠分。
- 多个和牌拆分具有相同倍率时，引擎按固定搜索顺序选择一个拆分。
- 点炮和牌结算前，引擎从牌河移除最新的一张实体弃牌。一炮多响只移除一次；第一名之后的和牌者使用同一牌张的逻辑引用，因此不会重复消耗实体牌。
- 自摸和单人点炮胡由和牌者下家继续。一炮多响按出牌者下家起的座次顺序结算，并由出牌者下家继续。
- 普通摸牌从墙头取得。杠后补牌从墙尾取得。不设置王牌或死墙。
- 最后一张牌的当前动作和响应窗口完成后终局。杠已成立但无牌可补时，保留杠和杠分，然后终局。
- 排名先按分数从高到低排序。同分时，绝对座位编号较小者在前。

## step 语义

`Game::new(seed)` 和 `reset(seed)` 完成洗牌、发牌和不需要外部选择的推进，并停在第一个换牌决策。

一次 `step(action)` 只接受当前决策者的一个动作。引擎随后自动完成摸牌、轮转、无人响应、计分和终局检查，直到下一个决策或终局。即使当前只有一个合法动作，调用者仍必须提交该动作。因此，动作序列和 step 数可以稳定重放。

普通回合没有"空过"。玩家摸牌后必须弃牌、胡或杠。`Pass` 只在胡牌或面子响应窗口合法。非法动作返回错误，并且不得修改状态或随机数进度。

## 固定动作空间

策略使用 115 维固定动作空间：

| ID | 动作 |
| --- | --- |
| `0..26` | 选择一张换出牌 |
| `27..29` | 选择缺门 |
| `30..56` | 弃牌 |
| `57` | 胡 |
| `58` | 碰 |
| `59` | 直杠 |
| `60..86` | 按牌种选择暗杠 |
| `87..113` | 按牌种选择碰杠 |
| `114` | 过 |

`ActionMask` 使用两个 little-endian `u64`。未使用的高位必须为零。结构化 `LegalActions` 和固定 mask 必须逐项一致。

引擎使用三个直方图表达胡后的持牌状态：`concealed` 保存全部暗牌和历次和牌张，`locked` 保存和牌基础与历次和牌张，`win_base` 只保存和牌基础。因此，活动牌等于 `concealed - locked`，公开的历次和牌张等于 `locked - win_base`，后续和牌可用牌等于活动牌加 `win_base`。该数据结构直接排除了历史和牌张替换和牌基础的可能。

首次和牌时，引擎将选中的完整暗牌结构加入 `locked`，再从 `win_base` 中分离本次和牌张。后续和牌必须完整包含 `win_base`，并将新的和牌张加入公开历史。历史和牌张不能参与弃牌、后续和牌或面子动作。

未和牌玩家的弃牌 mask 包含全部活动牌，不因缺门牌而收窄。持有缺门牌时不提供胡牌动作。已和牌玩家在普通摸牌后只能弃本次摸牌，但仍可选择和牌、暗杠或碰杠；该玩家不能响应他人的碰或直杠。

暗杠和碰杠可以消耗活动牌与 `win_base`，但不能消耗公开的历次和牌张。碰杠不要求第4张牌是当前回合摸到的牌。杠消耗 `win_base` 后，引擎在杠后流程中恢复与公开面子数量匹配的基础长度。

玩家因他人的弃牌获得直杠选项时，引擎立即将该牌种写入玩家的永久禁杠 mask。该 mask 同时禁止此后的暗杠和碰杠。玩家摸牌后的合法动作中出现碰杠选项时，引擎将本次摸到的牌种记为待提交禁用；当前 observation 已显示该禁用，当前 legal mask 仍保留已经出现的碰杠动作。`step` 处理当前选择前，将待提交禁用写入永久 mask。因此，无论玩家执行碰杠、和牌、其他杠或弃牌，该禁用都会保留。

`Game` 分别保存庄家是否已经普通弃牌，以及地胡窗口是否仍打开。庄家在首次普通弃牌前的自摸都可成立天胡；杠不会关闭该条件，所以杠后补牌自摸仍可成立天胡。闲家只有在自己的第一次普通摸牌自摸时才可成立地胡。任何碰、任何杠或和牌都会永久关闭地胡窗口；普通弃牌和放弃响应不会关闭该窗口。

## 信息边界

`Game` 保存四家合并持牌、历次和牌张和牌墙顺序。以下接口用于建立策略可见边界：

- `StepOutcome::for_player(viewer)` 隐藏其他玩家的摸牌牌面；
- `Game::observation_into` 只写指定观察者可见的数据；
- `Game::events_into` 和 `step_events_into` 旋转座位并过滤私有事件；
- `Game::resample_information_set` 保持当前行动者的 observation 和 legal mask 不变，再采样未知暗牌与牌墙；
- `Game::resample_live_wall` 保持全部手牌和公开状态不变，只重排尚未摸出的牌墙。

响应窗口的合法响应集合依赖暗手。通用 `resample_information_set` 因此固定响应窗口中的四家手牌，只重排未来牌墙。策略需要更强的响应后验时，必须重新构造响应窗口，不能复用已经泄露合法性的内部集合。

事件历史为每局 512 条固定宽度环形记录。事件数组使用观察者相对座位。摸牌牌面只对摸牌者可见。终局没有行动者时，observation 以庄家作为相对座位 `0`。

`RuleNn::action` 只能通过 `Game::observation_into`、`Game::events_into` 和 legal mask 获取策略输入。ONNX 图不接收 `Game`、对手暗手或牌墙。图输出 115 个原始 logits，Rust core 在推理后应用 legal mask。该边界使模型无法选择非法动作，也防止模型绕过观察者信息过滤。

## 批量执行

`Batch` 保存多个独立 `Game`，并在批量足够大时使用 Rayon。`*_into` 接口由调用者提供输出缓冲区，避免热路径分配。主要能力包括重置、克隆、按索引删除、信息集与 live-wall 重采样、合法动作/向听分析/observation/事件写入，以及普通、masked 和融合 step。

两种内置规则策略和 `rule-nn` 都提供 `Batch` 接口。锦标赛对所有策略使用单局接口，并在牌局层并行执行，不通过 `Batch` 调用。

## 策略约束

- 所有策略必须返回 legal mask 中的动作；
- 策略不得读取对手身份；
- 策略强弱通过独立 seed block 的锦标赛测量，方法见 [`engine/tools/rule-tournament/README.md`](engine/tools/rule-tournament/README.md)。

## 计分执行

引擎先计算最高牌型倍率，再收集本次实际成立的全部状态/事件倍率。最终和牌倍率取牌型倍率、自摸时的2倍基准和每个状态/事件倍率的最大值。引擎保留所有成立的事件标记用于事件输出，但不会把这些倍率相乘。高于2倍的牌型自摸时不再额外翻倍。

同一付款事件有多个收款者时，引擎从付款者下家开始按座次逐笔转账。每笔实际付款为应付金额和付款者当前分数的较小值。分数不能低于零，且总分保持不变。

牌墙耗尽后先查花猪，再查大叫。每个阶段按付款者座位 `0..3` 处理。每名付款者完成该阶段全部付款后，引擎检查是否已有三家分数为零。

花猪由终局时是否仍持有声明的缺门花色判断。每名花猪向每名非花猪付款。花猪结算不会把任何玩家排除出随后的查大叫。

查大叫只使用终局当前持牌的听牌状态和最大结构倍率。已经胡过的玩家按当前和牌基础、活动牌和公开面子计算；历次和牌张与历史最高和牌倍率均不参与。查大叫不计算自摸、抢杠胡、杠上炮、杠上开花、海底捞月、天胡或地胡等状态/事件番，也不要求待牌在剩余牌墙中仍有副本。

引擎不执行退税。终局结算不会撤销或退回此前已经结算的杠分。引擎也不执行呼叫转移。杠后点炮不会撤销杠分，也不会把杠分转移给和牌者。
