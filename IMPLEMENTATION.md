# 引擎实现约定

`rules.md` 是玩法与计分的唯一依据。本文只固定规则未指定、但引擎为了确定性必须选择的行为；不增加过胡、封顶、退税等额外规则。

## 确定性约定

- 座位编号为 `0..3`，庄家固定为 `0`，行牌方向按编号递增并循环。需要在多人之间确定顺序时，从事件来源者的下家开始按座次处理。
- 洗牌和换三张方向只使用局种子决定。换牌方向在左、对家、右中等概率选择；相同种子和相同动作序列必须得到相同结果。
- 换三张按每家连续三次选择一张牌，共 12 个外部决策；首张确定花色，后两张的合法动作只含同花色且尚未选完的持牌。四家选满后统一交换。定缺仍是每家一次三选一，四家选满后统一公开。多人和牌响应先收齐选择再统一生效。碰杠响应按优先座次依次请求，第一个接受者立即生效；未完成的同时选择不向其他玩家泄露。
- 一张弃牌先处理所有人的和牌响应。只要有人和，碰与直杠全部取消；无人和时，碰与直杠冲突由离出牌者最近的响应者取得。`Pass` 只表示在响应窗口放弃可选的和、碰或杠，正常轮次不存在空过。规则未规定的“过胡”“过碰”等后续限制不实现。
- 抢杠胡使用同样的多人和牌处理。有人抢杠时碰杠不成立、不计杠分；无人和才完成碰杠并计分。
- 多种拆分均可和时，选择总倍率最高的拆分。总倍率相同则按固定牌序选择第一个拆分，不把拆分选择暴露为玩家动作。
- 每次和牌结构必须包含本次自摸牌或他人提供的和牌张。结构中的牌全部锁定；已锁定牌可在后续和牌结构中再次使用，也可在已有碰牌上补成碰杠，但不能暗杠或普通弃出。
- 点炮和牌张成为和牌者可继续使用的锁定牌。一炮多响时，每名和牌者各自持有该牌的逻辑引用；弃牌记录中仍只有一张物理牌。
- 自摸后由和牌者下家继续。多人点炮和牌按出牌者下家起的座次顺序结算，由最后一名和牌者的下家继续；单人点炮是同一规则的特例。
- 普通摸牌从墙头取，杠后补牌从墙尾取，不设王牌或死墙。最后一张牌的当前动作和响应窗口处理完再终局；杠已成立但无牌可补时，保留杠及其计分并直接终局。
- 同一付款事件有多个收款者时，从付款者下家开始按座次逐笔支付。终局先查花猪、后查大叫；每阶段付款者按 `0..3` 处理。每笔只转移付款者当前可支付的分数，完整事件结束后再检查“三家为 0”并提前终局。
- 查大叫的最大倍率只取可组成的牌型倍率，不假设自摸、天胡、地胡、杠上或海底等事件番，也不检查待牌在墙内是否仍有剩余。对已经和过的玩家，其既有锁定和牌结构也纳入最大值候选。

## 状态与 step 语义

- `reset(seed)` 完成洗牌、发牌和所有无需策略的推进，停在第一个玩家决策点。
- 一个 `step(action)` 接受当前决策者的一个合法选择，随后自动执行摸牌、轮转、无人响应、计分和终局检查等非决策事件，直到下一个策略决策点或终局。
- 普通轮次强制摸牌，随后必须弃牌、和牌或杠，不能什么都不做。每次 `step` 明确返回本次摸牌和弃牌事件；权威环境收到完整事件，交给策略前按观察者过滤，摸牌牌面只进入摸牌者的观察，弃牌事件进入所有玩家的公开观察。
- 每个策略动作都是一个外部决策，即使当时只有一个合法动作也必须由外部提交；引擎只自动推进不需要玩家选择的强制转换。这样同一动作序列的 step 数保持稳定。
- 决策状态必须给出阶段、行动座位和完整合法动作集合。训练接口使用固定 115 维动作空间：`27 换牌 + 3 定缺 + 27 弃牌 + 胡/碰/直杠 + 27 暗杠 + 27 碰杠 + Pass`；暗杠动作位只在该玩家尚未和牌时开放，mask 与结构化合法动作必须逐位一致。定缺尚未打完时只能弃缺门牌；仍有缺门牌时不提供和牌动作；锁定牌不提供普通弃牌动作。
- 非法动作返回错误且状态不变。终局状态不再接受动作。
- 收集型阶段按 `0..3` 请求换三张和定缺；响应型阶段从事件来源者下家开始请求。已提交的选择只保存在内部，收齐后一次性解析。

## Python FFI

- Rust workspace 位于 `engine/`：`engine/core` 只包含规则和批量环境，`engine/pybind` 提供 PyO3 0.27 + NumPy 0.27 扩展，Python 模块名为 `bloodflow_mahjong`。
- 训练热路径使用 `Batch.step_and_observe_history_into`。一次 Rayon 遍历直接写 `records[B,12]`、`mask_words[B,2]`、状态观测和历史；`history_seat_masks[B]` 只让下一行动者属于 learner/冻结 Transformer 的行写历史。没有 NumPy 中间复制或 Rust 临时 `Vec`，Rust 计算阶段释放 GIL。
- 事件流由 Rust 为每局维护 512 条固定宽度环形记录，按 viewer 输出时旋转座位并过滤私有动作和暗摸牌面。训练窗口写最近 192 条；`event_dropped` 用于监控历史环覆盖。终局通过 `Batch.reset_and_observe_history_into` 只重置并刷新命中的行，不再重新扫描整个 batch。
- `records` 列为 `[draw_player, draw_tile, replacement, discard_player, discard_tile, score_delta[4], next_actor, next_phase, terminal]`；缺失对象使用 `-1`。`mask_words` 是 115 维动作空间的两个 little-endian `u64`，高位未使用位始终为零。
- 观测以当前行动者为相对座位 `0`；终局使用庄家作为相对座位 `0` 且 `meta[1] = -1`。`tile_obs` 的 10 个 plane 依次为行动者手牌、行动者已选换牌、四家锁牌、四家弃牌计数；`melds` 是 `[tile, kind, source_relative]`；`river` 是按时间顺序的 `[tile, owner_relative]`，空槽为 `255`。
- `meta[0..34]` 依次为阶段、绝对行动者、相对庄家、换牌方向、墙剩余、摸牌信息、响应来源/牌、牌河长度、换牌进度、四家分数、缺门、胡牌标记、手牌数量、终局标记、响应 flags，以及四家历史最大胡牌倍率。完整索引表见 [`engine/pybind/README.md`](engine/pybind/README.md)。
- 所有 into API 严格检查 dtype、shape、C-contiguous 和对齐；重叠 NumPy view、非法动作或错误长度在状态推进前返回错误。向听标签使用 `Batch.hand_analysis_indices_into`，不会为了 learner 子集分析整批环境。

构建与验证：

```bash
maturin develop --release --manifest-path engine/pybind/Cargo.toml
cargo test --manifest-path engine/Cargo.toml --workspace --all-targets
python -m pytest engine/pybind/tests
```

## 最小验收矩阵

| 场景 | 操作 | 必须满足 |
|---|---|---|
| 发牌与复现 | 用同一种子重置两局 | 庄家 14 张、其余各 13 张；墙和换牌方向一致；相同动作序列的状态与分数一致 |
| 换三张与定缺 | 四家各连续选择三张同花色牌并定缺 | 每家前两步仍由本人决策且 mask 锁定花色；第 12 步后换牌同时生效且方向一致；收到的牌正确；缺门未清空前弃牌掩码只含缺门牌 |
| Legal Action Mask | 遍历所有阶段和全部 115 个动作编号 | 每一位与结构化动作合法性一致；非法位提交不改变状态；Batch 输出与单环境一致 |
| 天胡与地胡 | 庄家起手可和；闲家可和庄家首张弃牌 | 分别得到 32 倍事件番；闲家已首次摸牌后不再得到地胡 |
| 点炮优先级 | 同一弃牌可被一人和、另一人碰或杠 | 只执行和牌；点炮者支付；碰杠不改变状态 |
| 一炮多响 | 两家同时选择和 | 两家都按各自总倍率收款；付款逐笔且不低于 0；续行座位符合固定座次顺序 |
| 碰杠冲突 | 无人和，同一弃牌有多人申请碰或直杠 | 仅离出牌者最近者成功；碰后进入其弃牌决策，直杠后从墙尾补牌 |
| 抢杠胡 | 碰杠牌可使一人或多人和 | 和牌全部成立；碰杠取消且不收杠分 |
| 杠与事件番 | 分别完成直杠、碰杠、暗杠，并测试补牌和、补牌后首弃被和 | 杠分分别为 `1/1/2` 倍规则；杠上开花或杠上炮再乘 2，不呼叫转移 |
| 和牌后继续 | 玩家和牌后轮到其下家，之后再次获得可和牌张 | 首次结构保持锁定且不可弃；锁定牌可复用于第二次和牌或已有碰牌的碰杠；和牌后暗杠动作始终不可用；第二次独立计分 |
| 牌型与倍率 | 对 `rules.md` 中每个牌型提供正例和只差一个条件的反例 | 普通结构、七对和全部衍生牌型识别正确；每个包含族只取最高具体名称；不重复乘基础性质；独立事件番仍可相乘 |
| 海底与空墙 | 最后一张普通摸牌自摸；或杠后无牌可补 | 前者获得海底 2 倍；后者保留已成立的杠分并直接进入终局 |
| 查花猪与查大叫 | 终局同时存在花猪、未听牌者、听牌者和已和牌者 | 花猪只查花猪且先结算；其余未听者按收款方最大牌型倍率赔付；分数始终非负且总分守恒 |
| 非法动作 | 提交非当前座位动作、非法换牌、被定缺/锁牌禁止的弃牌 | 返回错误；局面、随机状态和分数完全不变 |
